@model IEnumerable<Sistema_de_Informes_de_Analisis_Financieros.Models.Sector>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nomsector)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nomsector)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idsector">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idsector">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idsector">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
